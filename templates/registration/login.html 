{% extends "store/_base.html" %}

{% block title %}Sign in | Opera Home{% endblock %}

{% block content %}
<section class="hero" aria-label="Sign in to Opera Home">
  <div class="hero__in">
    <h1>Sign in</h1>
    <p class="lead">
      Access your saved cart and keep your Opera Home experience in sync.
    </p>
  </div>
</section>

<section class="container section" aria-label="Login form">
  <div class="card" style="max-width:420px;margin:0 auto;">
    <form method="post" novalidate>
      {% csrf_token %}
      {% if form.non_field_errors %}
        <div class="alert">
          {{ form.non_field_errors }}
        </div>
      {% endif %}

      <div class="form-row" style="flex-direction:column;gap:12px;">
        <div>
          <label for="{{ form.username.id_for_label }}">Email or username</label>
          {{ form.username }}
        </div>
        <div>
          <label for="{{ form.password.id_for_label }}">Password</label>
          {{ form.password }}
        </div>
      </div>

      {% if next %}
        <input type="hidden" name="next" value="{{ next }}">
      {% endif %}

      <div class="hero__actions" style="margin-top:16px;">
        <button type="submit" class="btn btn--gold">Sign in</button>
      </div>
    </form>
  </div>
</section>
{% endblock %}
